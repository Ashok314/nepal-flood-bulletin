# The public people-search site needs NO environment variables and NO database.
# It fetches the live feed at request time and caches it in memory.
#
# This file is intentionally minimal. Copy to .env only if you need to override
# a default in src/lib/config.ts during local development.
